{
  "name": "learning-blog",
  "version": "1.0.0",
  "private": true,
  "description": "A zero-dependency static blog for course notes and project writeups.",
  "scripts": {
    "build": "node scripts/build.js",
    "new": "node scripts/new-post.js",
    "dev": "node scripts/build.js --watch --serve",
    "preview": "node scripts/build.js --serve"
  }
}
